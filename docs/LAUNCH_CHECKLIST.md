# ⚠️ Deprecated

This checklist described a fictional submission flow
(`marketplace.anthropic.com` is not a real Anthropic submission path) and
generic boilerplate that doesn't reflect how this project is actually
packaged or submitted.

For the real, current steps, see [`PACKAGING.md`](../PACKAGING.md) in the
repo root — it covers bundling the `mcp` dependency, installing the MCPB
CLI, validating and packing `manifest.json` into a `.mcpb` file, and where
Desktop Extension submissions actually go (a separate form from the
Connectors Directory portal, which is for remote HTTPS servers only).

This file can be safely deleted (`rm docs/LAUNCH_CHECKLIST.md`).
